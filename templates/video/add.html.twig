{% extends 'base.html.twig' %}

{% block title %}Ajouter une vidéo{% endblock %}

{% block body %}
<h2>Ajout vidéos</h2>
{# <form action="{{ path('app_video_add', {'trickId': trick.id}) }}" method="POST">
    <label for="video_url">URL de la vidéo :</label>
    <input type="text" id="video_url" name="video_url" required>
    {# <button type="submit">Ajouter la vidéo</button> #}
{# </form>  #}

<form action="{{ path('app_video_add', {'trickId': trick.id}) }}" method="POST" enctype="multipart/form-data">
    <div>
        <label for="video1">Vidéo 1 :</label>
        <input type="text" id="video1" name="videos[]">
    </div>
    <div>
        <label for="video2">Vidéo 2 :</label>
        <input type="text" id="video2" name="videos[]">
    </div>
    <div>
        <label for="video3">Vidéo 3 :</label>
        <input type="text" id="video3" name="videos[]">
    </div>

    {# <button type="submit">Ajouter les vidéos</button> #}
</form>

<h2>Vidéos associées</h2>
<ul>
    {% for video in trick.videos %}
        <li>
            <iframe src="{{ video.name }}" frameborder="0" allowfullscreen></iframe>
        </li>
    {% else %}
        <li>Aucune vidéo associée.</li>
    {% endfor %}
{% endblock %}
